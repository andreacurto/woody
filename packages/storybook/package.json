{
  "name": "@woody-ui/storybook",
  "version": "0.0.0",
  "private": true,
  "description": "Storybook per Woody — documentazione interattiva dei componenti",
  "license": "MIT",
  "scripts": {
    "dev": "echo 'Storybook dev: da configurare'",
    "build": "echo 'Storybook build: da configurare'",
    "clean": "rm -rf storybook-static"
  },
  "dependencies": {
    "@woody-ui/css": "workspace:*",
    "@woody-ui/react": "workspace:*"
  }
}
